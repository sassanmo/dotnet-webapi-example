services:
  mongo:
    image: mongo:7.0
    container_name: todo-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: todo
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data:
